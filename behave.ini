[behave]

lang = es # Palabra clave en español (Escenario, Dado, Cuando,...)
default_tags = ~@wip #Excluye en progreso por defecto
paths = features/ #Carpeta donde se encuentran los features
color = true #Colorea la salida en consola